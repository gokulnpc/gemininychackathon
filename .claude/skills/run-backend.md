Start the FastAPI backend server in hot-reload dev mode.

```bash
cd /Users/gokuleshwarannarayanan/Documents/Repositories/Work\ Repositories/google-hackathon/backend && python -m uvicorn main:app --reload --port 8000
```

Docs available at: http://localhost:8000/docs
Health check: http://localhost:8000/health
